/** @type {import('next').NextConfig} */
const nextConfig = {
  // Specify the root directory for Next.js
  // outputFileTracingRoot: __dirname, // Removed due to ES module incompatibility
  // Other configuration options can be added here
};

export default nextConfig;